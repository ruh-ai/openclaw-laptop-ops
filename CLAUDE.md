# Claude Code — read AGENTS.md

This repo's single instruction layer is `AGENTS.md`. Read it first, then `RUNBOOK.md`.
Do not improvise outside the runbook's current phase. Rules in `AGENTS.md` about
protected paths (Tailscale, SSH, firewall, startup task) are absolute.
